import type { Metadata } from "next";
export const metadata: Metadata = { title: "Benefit workspace", description: "Verify tokenized stock ownership and claim benefits on Base." };
export default function AppLayout({ children }: { children: React.ReactNode }) { return children; }
